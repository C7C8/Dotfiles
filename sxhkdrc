#
# SXHKD CONTROLS
#
# These controls manipulate most things on my system, designation
# lazarus3 (make of that symbolism whatever you want...). They are
# approximately ordered by criticality: core commands are first,
# WM controls, launchers, etc.
#
#


# =====================================
# CRITICAL COMMANDS
#

# Launch a terminal. End-all fix-all for the worst
# things that could possibly happen.
XF86Launch3
	terminator

# Quit bspwm normally.
super + alt + Escape
	bspc quit

# Close and kill windows
super + {_,shift + }q
	bspc node -c

# Focus or send to the given desktop
super + {_,shift + }{1-9,0}
	bspc {desktop -f,node -d} '^{1-9,10}'

# Make sxhkd reload its configuration files:
super + Escape
	pkill -USR1 -x sxhkd


# =====================================
# LAYOUT COMMANDS
#

# Swap the current node and the biggest node
super + g
	bspc node -s biggest

# Rotate nodes
super + r
	bspc node -R 90

# Mirror & flip leaves of a tree
super + {comma, period}
	bspc node -F {vertical, horizontal}


# =====================================
# VIEWING/WINDOW COMMANDS
#

# Set the window state
super + {t,shift + t,s,f}
	bspc node -t {tiled,pseudo_tiled,floating,fullscreen}

# Set the node flags
super + ctrl + {x,y,z}
	bspc node -g {locked,sticky,private}


# =====================================
# FOCUS COMMANDS
#

# Focus the node in the given direction
super + {_,shift + }{h, j, k, l}
	bspc node -{f,s} {west,south,north,east}

# Focus the node for the given path jump
super + {a,b}
	bspc node -f @{parent,brother}

~button1
	bspc pointer -g focus


# =====================================
# PRESELECTION COMMANDS
#

# Preselect the direction
super + ctrl + {h, j, k, l}
	bspc node -p {west,south,north,east}

# Preselect the ratio
super + ctrl + {1-9}
	bspc node -o 0.{1-9}

# Cancel the preselection for the focused node
super + ctrl + space
	bspc node -p cancel

# Cancel the preselection for the focused desktop
super + ctrl + shift + space
	bspc query -N -d | xargs -I id -n 1 bspc node id -p cancel


# =====================================
# MOVE/RESIZE COMMANDS
#

# expand a window by moving one of its side outward
super + alt + {h, j, k, l}
	bspc node -z {left -20 0,bottom 0 20,top 0 -20,right 20 0}

# contract a window by moving one of its side inward
super + alt + shift + {h, j, k, l}
	bspc node -z {right -20 0,top 0 20,bottom 0 -20,left 20 0}

# move a floating window
super + ctrl + {h,j,k,l}
	bspc node -v {-20 0,0 20,0 -20,20 0}


# =====================================
# CUSTOM APPLICATION LAUNCHERS AND 
# HOTKEYS

super + Return
	terminator
super + d
	synapse
super + ctrl + f
	pcmanfm -n
super + ctrl + v
	gvim -c "call ToggleGUI()"
super + ctrl + shift + d 
	gvim -c "call ToggleGUI()"&& \
	gvim -c "call ToggleGUI()" && \
	bspc node -p south && \
	gvim -c "call ToggleGUI()" && \
	bspc node -f east && \
	bspc node -p south && \
	gvim -c "call ToggleGUI()" && \
	bspc node -f @parent && \
	bspc node -f @parent && \
	bspc node -p south && \
	bspc node -o 0.9 && \
	terminator
super + ctrl + d 
	gvim -c "call ToggleGUI()" && \
	gvim -c "call ToggleGUI()" && \
	bspc node -f @parent && \
	bspc node -p south && \
	bspc node -o 0.9 && \
	terminator
super + ctrl + m
	pcmanfm -n ~/Music & vlc
super + ctrl + p
	pavucontrol
super + ctrl + w
	wicd-gtk
XF86MonBrightnessDown
	light -U 10
XF86MonBrightnessUp
	light -A 10
XF86AudioLowerVolume
	amixer -D pulse sset Master 5%-
XF86AudioRaiseVolume
	amixer -D pulse sset Master 5%+
XF86AudioMute
	amixer -D pulse sset Master 0%
super + m
	i3lock -i ~/Dotfiles/media/lock.png
	setxkbmap us -variant colemak


#
# UX MODES
#

super + ctrl + s
	~/Dotfiles/modes/switch_standby.sh
super + ctrl + b
	~/Dotfiles/modes/switch_battle.sh
super + ctrl + shift + f
	~/Dotfiles/modes/switch_freespace.sh

#
# MISC. SOUNDBOARD STUFF 
# (for no particular reason)
#

super + shift + ctrl + s
	~/Dotfiles/soundboard/playsound.sh ~/Dotfiles/soundboard/cymcrash.ogg
super + shift + ctrl + m
	~/Dotfiles/soundboard/playsound.sh ~/Dotfiles/soundboard/martian.ogg
super + shift + ctrl + e
	~/Dotfiles/soundboard/playsound.sh ~/Dotfiles/soundboard/exterminate.ogg
